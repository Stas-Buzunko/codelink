language: node_js
node_js:
- stable
cache:
  directories:
  - node_modules
script:
- npm test
- npm run build

after_success:
  - npm install -g firebase-tools
  - firebase deploy — project test-5eae3 — token $FIREBASE_TOKEN.
